# OPS Console backend — Phase 1
# SQLite comes from the Python standard library; no database server is needed.
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7

# reading the fingerprint terminal's legacy .XLS export.
# Pinned to 1.x on purpose: xlrd 2.x removed .xls support entirely.
xlrd==1.2.0

# test dependencies
pytest>=8.0
httpx>=0.27
xlwt==1.3.0   # builds .xls fixtures at run time, so none are committed

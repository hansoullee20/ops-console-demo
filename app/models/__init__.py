"""Row/dataclass models for the SQLite schema.

The schema itself is defined by the SQL migrations in app/migrations, which are
the single source of truth. Typed accessors land here as phases add them."""

"""Test fixtures built at run time.

The real terminal export contains employee names and their movements, so it is
never committed. `terminal_xls.py` writes byte-compatible workbooks from
fictional data instead, which keeps the format documented in code and keeps
personal data out of the repository.
"""

name: Harden Okja failure paths (legacy)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy workflow retired
        run: |
          echo "Failure-path patches are now maintained directly on aihub-voice-test."
          echo "Use the deterministic pre-build failure matrix before producing an APK."
